<!doctype html>
<html lang = eng>

<head>
    <title>Tiny App</title>
</head>

<body>
    <ul>
        <% for(url in urls){ %>
            <li><%= url %>: <%= urls[url] %>  <a href="#">Shortened URL</a></href></li>               
        <% } %>
    </ul>
</body>

</html>
