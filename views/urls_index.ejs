<!doctype html>
<html lang = eng>

<head>
    <% include partials/_header %>
</head>

<body>
    <ul>
        <% for(url in urls){ %>
            <li>
                <form method="POST" action="/urls/<%= url %>/delete">
                <%= url %>: <%= urls[url] %>  
                <input type="submit" value = "Delete">
                </form>
            </li>
        <% } %>
    </ul>
    <a href="/urls/new">Shorten another URL</a></href>
</body>

</html>
