<!doctype html>
<html lang = eng>

<head>
    <% include partials/_header %>
</head>

<body>
    <ul>
        <% for(url in urls){ %>
            <li>
                <form method="POST" action="/urls/<%= url %>/delete">
                <%= url %>: <%= urls[url] %>  <a href="#">Shortened URL</a></href>
                <input type="submit" value = "Delete">
                </form>
            </li>
        <% } %>
    </ul>
</body>

</html>
